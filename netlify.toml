[build]
  command = "npm ci && npm run build"
  publish = "dist/spa"

[build.environment]
  NODE_VERSION = "18"

# Ensure SPA history fallback works (alternative to public/_redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


